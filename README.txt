launch the app using : 'docker-compose up -d --build'
kill the app using : 'docker-compose down'

Notes:

The website is almost empty since I put my focus on the setup with docker rather than the functionnalities of the website.
